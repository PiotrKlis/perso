import 'package:flutter/material.dart';

class TrainingFinishedScreen extends StatelessWidget {
  const TrainingFinishedScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Training finished screen'),
    );
  }
}
